# Calculate exec=/bin/bash ini(overlay-container.revision)<#-cut(0,-,#-cl_pass_file-#)-#

#-ini(overlay-container.revision,#-cut(0,-,#-cl_pass_file-#)-#)-#

filecap /bin/ping net_raw &>/dev/null
filecap /bin/arping net_raw &>/dev/null
filecap /sbin/unix_chkpwd dac_override &>/dev/null

exit 0
